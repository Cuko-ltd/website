node_modules/
dist/
.astro/
.DS_Store
.env
.env.*
!.env.example
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.vscode/

# Playwright
test-results/
playwright-report/
blob-report/
playwright/.cache/

# Visual regression baselines — regenerate locally with `npm run test:update`
tests/visual.spec.ts-snapshots/

# Claude Code — agent runtime memory (regenerates per session, may include raw prompt context)
.claude/agent-memory/

# Internal — project agents and internal planning docs (private to operator)
.claude/agents/
doc/blog-outlines/
doc/marketing/
